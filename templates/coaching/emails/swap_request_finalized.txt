Hi {{ recipient.first_name|default:recipient.username }},

This is an update regarding the coach swap request for the session on {{ swap_request.session.start_time|date:"F j, Y" }}.

The final status is: {{ swap_request.get_status_display }}.

If accepted, the new coach for this session is {{ swap_request.session.coach.get_full_name|default:swap_request.session.coach.username }}.

Best,
The JH Motiv LTD Team